using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourcesManager : MonoBehaviour{

	BuildingDB buildingDB;
	ResourceDB resourceDB;
	// Use this for initialization
	void Start () {
		//resourceDB = new Resources(100,100,100,5,0,5,1,1);
		//buildingDB = new BuildingDB(resourceDB);
		//TODO: get initialiation values from properties file(class: Properties.cs)
	}
	
	// Update is called once per frame
	void Update () {
		//resourceDB.updateAllResourcesRepeat();
		//TODO: Create lows(math or not) which will be to change resources toge!  	
	}
}
